# Secrets / env
.env
.env.*
*.key
*.pem

# Per-device enrolment tokens / real router exports (never commit these).
# NOTE: agent/*.rsc are DRAFT templates and ARE tracked. Real device exports
# (with serials, creds, public IPs) must not be — keep them out of agent/.
secrets/
*-export.rsc
*.backup

# Node (when ingest/ and worker/ land)
node_modules/
.next/
dist/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# Logs / scratch
*.log
/tmp/
